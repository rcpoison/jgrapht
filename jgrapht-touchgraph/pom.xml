<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.sf.jgrapht</groupId>
		<artifactId>jgrapht</artifactId>
		<version>0.8.3-SNAPSHOT</version>
	</parent>
	<artifactId>jgrapht-touchgraph</artifactId>
	<name>JGraphT - Touchgraph</name>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>1.5</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<finalName>${project.artifactId}-${project.version}-shade</finalName>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>jgrapht-ext</artifactId>
		</dependency>
		<dependency>
			<groupId>com.touchgraph</groupId>
			<artifactId>TGGraphLayout</artifactId>
			<version>UNKNOWN</version>
		</dependency>
	</dependencies>
	<repositories>
		<repository>
			<!-- These dependencies are not available in any known maven
				repository, and have been placed in the source tree directly.
				This makes this module (jgrapht-touchgraph) unsuitable for
				submission to central. -->
			<id>3rd-party-dependencies</id>
			<url>file:${basedir}/3rdparty</url>
		</repository>
	</repositories>
</project>
